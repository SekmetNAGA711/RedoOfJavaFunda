package unit1_fundamentals.sec6;

public class ConditionalStatements {

    public static void main(String[] args) {

        /*operators (number left bigger than number on the right - false*Test
        > greater than
        >= greater than or equal to
        <less than
        <= less than or equal to

        Equality operators
        == equal to
        != not equal to
        */

if(true) {} //when the condition in the brackets is true the program in the curly brackets ie the body will run if not it will not




    }



}
